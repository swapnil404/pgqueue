services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: pgqueue_dev
    ports:
      - "5436:5432"
    volumes:
      - pgqueue_data:/var/lib/postgresql/data

volumes:
  pgqueue_data:
